import type{Citation}from"../types/api";function valid(c:Citation){if(!c.id&&!c.source_id)return false;if(c.source_id==="pending-index"||c.source_id==="general-knowledge")return false;return Boolean(c.url||c.path||c.document_number)}export function CitationList({items}:{items:Citation[]}){const x=items.filter(valid);if(!x.length)return null;return <section><h3>استنادها</h3>{x.map((c,i)=><div key={c.id||c.source_id||i}>{c.url?<a href={c.url} target="_blank">{c.title||c.source_id}</a>:<span>{c.title||c.source_id} — {c.path||c.document_number}</span>}</div>)}</section>}