import type { Citation } from "../types/api";
function valid(c:Citation){if(!c.id && !c.source_id) return false;if(["pending-index","general-knowledge","knowledge-base-not-indexed","internal-knowledge"].includes(c.source_id||"")) return false;return Boolean(c.url||c.path||c.document_number)}
export function CitationList({items}:{items:Citation[]}){const x=items.filter(valid);if(!x.length)return null;return <section aria-label="استنادها"><h3>استنادها</h3>{x.map((c,i)=><div key={c.id||c.source_id||i}>{c.url?<a href={c.url} target="_blank" rel="noreferrer">{c.title||c.source_id}</a>:<span>{c.title||c.source_id} — {c.path||c.document_number}</span>}</div>)}</section>}